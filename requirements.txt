# BrainCharge dependencies.
#
# This replaces a machine-generated `pip freeze` dump of an unrelated conda
# environment (hundreds of packages with local absolute file paths that only
# resolved on one machine) with the actual packages this project imports.
#
# Torch/CUDA note: install PyTorch yourself first following
# https://pytorch.org/get-started/locally/ for your platform (CPU-only vs
# CUDA build) -- pinning it here would fight that installer. Everything
# below assumes torch + torchvision are already present.

requests>=2.31
numpy>=1.26
opencv-python>=4.5.4      # needs FaceDetectorYN/FaceRecognizerSF for facial_recognition/
ultralytics>=8.4           # YOLO person/face detection (cv/picture.py)
hsemotion-onnx>=0.3.1      # facial emotion classifier (cv/hsemotion_detector.py)
transformers>=4.55         # voice emotion pipeline (voice_text_emotion/voice_emotion.py)
pygame>=2.6                # robot face display

# Optional but recommended -- enables VAD-based recording (natural turn-taking
# instead of a fixed recording duration). Can be finicky to install on
# Linux/macOS (needs PortAudio dev headers); main.py falls back to
# fixed-duration ffmpeg recording if this isn't available.
pyaudio>=0.2.14
